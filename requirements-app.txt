# EthicsBERT – Gradio demo dependencies (app.py)
# Run in a separate venv or Hugging Face Space.
#
# Install with:
#   pip install -r requirements-app.txt

torch==2.2.2
transformers==4.57.6
tokenizers==0.22.2
gradio==6.18.0
